jQuery.easing.jswing = jQuery.easing.swing;
jQuery.extend(jQuery.easing, {
    def: "easeOutQuad",
    swing: function (d, a, c, b, e) {
        return jQuery.easing[jQuery.easing.def](d, a, c, b, e)
    },
    easeInQuad: function (d, a, c, b, e) {
        return b * (a /= e) * a + c
    },
    easeOutQuad: function (d, a, c, b, e) {
        return -b * (a /= e) * (a - 2) + c
    },
    easeInOutQuad: function (d, a, c, b, e) {
        return (a /= e / 2) < 1 ? b / 2 * a * a + c : -b / 2 * (--a * (a - 2) - 1) + c
    },
    easeInCubic: function (d, a, c, b, e) {
        return b * (a /= e) * a * a + c
    },
    easeOutCubic: function (d, a, c, b, e) {
        return b * ((a = a / e - 1) * a * a + 1) + c
    },
    easeInOutCubic: function (d, a, c, b, e) {
        return (a /= e / 2) < 1 ? b / 2 * a * a * a + c : b / 2 * ((a -= 2) * a * a + 2) + c
    },
    easeInQuart: function (d, a, c, b, e) {
        return b * (a /= e) * a * a * a + c
    },
    easeOutQuart: function (d, a, c, b, e) {
        return -b * ((a = a / e - 1) * a * a * a - 1) + c
    },
    easeInOutQuart: function (d, a, c, b, e) {
        return (a /= e / 2) < 1 ? b / 2 * a * a * a * a + c : -b / 2 * ((a -= 2) * a * a * a - 2) + c
    },
    easeInQuint: function (d, a, c, b, e) {
        return b * (a /= e) * a * a * a * a + c
    },
    easeOutQuint: function (d, a, c, b, e) {
        return b * ((a = a / e - 1) * a * a * a * a + 1) + c
    },
    easeInOutQuint: function (d, a, c, b, e) {
        return (a /= e / 2) < 1 ? b / 2 * a * a * a * a * a + c : b / 2 * ((a -= 2) * a * a * a * a + 2) + c
    },
    easeInSine: function (d, a, c, b, e) {
        return -b * Math.cos(a / e * (Math.PI / 2)) + b + c
    },
    easeOutSine: function (d, a, c, b, e) {
        return b * Math.sin(a / e * (Math.PI / 2)) + c
    },
    easeInOutSine: function (d, a, c, b, e) {
        return -b / 2 * (Math.cos(Math.PI * a / e) - 1) + c
    },
    easeInExpo: function (d, a, c, b, e) {
        return a == 0 ? c : b * Math.pow(2, 10 * (a / e - 1)) + c
    },
    easeOutExpo: function (d, a, c, b, e) {
        return a == e ? c + b : b * (-Math.pow(2, - 10 * a / e) + 1) + c
    },
    easeInOutExpo: function (d, a, c, b, e) {
        return a == 0 ? c : a == e ? c + b : (a /= e / 2) < 1 ? b / 2 * Math.pow(2, 10 * (a - 1)) + c : b / 2 * (-Math.pow(2, - 10 * --a) + 2) + c
    },
    easeInCirc: function (d, a, c, b, e) {
        return -b * (Math.sqrt(1 - (a /= e) * a) - 1) + c
    },
    easeOutCirc: function (d, a, c, b, e) {
        return b * Math.sqrt(1 - (a = a / e - 1) * a) + c
    },
    easeInOutCirc: function (d, a, c, b, e) {
        return (a /= e / 2) < 1 ? -b / 2 * (Math.sqrt(1 - a * a) - 1) + c : b / 2 * (Math.sqrt(1 - (a -= 2) * a) + 1) + c
    },
    easeInElastic: function (d, a, c, b, e) {
        var d = 1.70158,
            h = 0,
            f = b;
        if (a == 0) return c;
        if ((a /= e) == 1) return c + b;
        h || (h = e * 0.3);
        f < Math.abs(b) ? (f = b, d = h / 4) : d = h / (2 * Math.PI) * Math.asin(b / f);
        return -(f * Math.pow(2, 10 * (a -= 1)) * Math.sin((a * e - d) * 2 * Math.PI / h)) + c
    },
    easeOutElastic: function (d, a, c, b, e) {
        var d = 1.70158,
            h = 0,
            f = b;
        if (a == 0) return c;
        if ((a /= e) == 1) return c + b;
        h || (h = e * 0.3);
        f < Math.abs(b) ? (f = b, d = h / 4) : d = h / (2 * Math.PI) * Math.asin(b / f);
        return f * Math.pow(2, - 10 * a) * Math.sin((a * e - d) * 2 * Math.PI / h) + b + c
    },
    easeInOutElastic: function (d, a, c, b, e) {
        var d = 1.70158,
            h = 0,
            f = b;
        if (a == 0) return c;
        if ((a /= e / 2) == 2) return c + b;
        h || (h = e * 0.3 * 1.5);
        f < Math.abs(b) ? (f = b, d = h / 4) : d = h / (2 * Math.PI) * Math.asin(b / f);
        return a < 1 ? -0.5 * f * Math.pow(2, 10 * (a -= 1)) * Math.sin((a * e - d) * 2 * Math.PI / h) + c : f * Math.pow(2, - 10 * (a -= 1)) * Math.sin((a * e - d) * 2 * Math.PI / h) * 0.5 + b + c
    },
    easeInBack: function (d, a, c, b, e, h) {
        h == void 0 && (h = 1.70158);
        return b * (a /= e) * a * ((h + 1) * a - h) + c
    },
    easeOutBack: function (d, a, c, b, e, h) {
        h == void 0 && (h = 1.70158);
        return b * ((a = a / e - 1) * a * ((h + 1) * a + h) + 1) + c
    },
    easeInOutBack: function (d, a, c, b, e, h) {
        h == void 0 && (h = 1.70158);
        return (a /= e / 2) < 1 ? b / 2 * a * a * (((h *= 1.525) + 1) * a - h) + c : b / 2 * ((a -= 2) * a * (((h *= 1.525) + 1) * a + h) + 2) + c
    },
    easeInBounce: function (d, a, c, b, e) {
        return b - jQuery.easing.easeOutBounce(d, e - a, 0, b, e) + c
    },
    easeOutBounce: function (d, a, c, b, e) {
        return (a /= e) < 1 / 2.75 ? b * 7.5625 * a * a + c : a < 2 / 2.75 ? b * (7.5625 * (a -= 1.5 / 2.75) * a + 0.75) + c : a < 2.5 / 2.75 ? b * (7.5625 * (a -= 2.25 / 2.75) * a + 0.9375) + c : b * (7.5625 * (a -= 2.625 / 2.75) * a + 0.984375) + c
    },
    easeInOutBounce: function (d, a, c, b, e) {
        return a < e / 2 ? jQuery.easing.easeInBounce(d, a * 2, 0, b, e) * 0.5 + c : jQuery.easing.easeOutBounce(d, a * 2 - e, 0, b, e) * 0.5 + b * 0.5 + c
    }
});
(function (d) {
    var a = {
        vertical: !1,
        rtl: !1,
        start: 1,
        offset: 1,
        size: null,
        scroll: 3,
        visible: null,
        animation: "normal",
        easing: "swing",
        auto: 0,
        wrap: null,
        initCallback: null,
        setupCallback: null,
        reloadCallback: null,
        itemLoadCallback: null,
        itemFirstInCallback: null,
        itemFirstOutCallback: null,
        itemLastInCallback: null,
        itemLastOutCallback: null,
        itemVisibleInCallback: null,
        itemVisibleOutCallback: null,
        animationStepCallback: null,
        buttonNextHTML: "<div></div>",
        buttonPrevHTML: "<div></div>",
        buttonNextEvent: "click",
        buttonPrevEvent: "click",
        buttonNextCallback: null,
        buttonPrevCallback: null,
        itemFallbackDimension: null
    }, c = !1;
    d(window).bind("load.jcarousel", function () {
        c = !0
    });
    d.jcarousel = function (e, b) {
        this.options = d.extend({}, a, b || {});
        this.autoStopped = this.locked = !1;
        this.buttonPrevState = this.buttonNextState = this.buttonPrev = this.buttonNext = this.list = this.clip = this.container = null;
        if (!b || b.rtl === void 0) this.options.rtl = (d(e).attr("dir") || d("html").attr("dir") || "").toLowerCase() == "rtl";
        this.wh = !this.options.vertical ? "width" : "height";
        this.lt = !this.options.vertical ? this.options.rtl ? "right" : "left" : "top";
        for (var f = "", k = e.className.split(" "), j = 0; j < k.length; j++) if (k[j].indexOf("jcarousel-skin") != -1) {
            d(e).removeClass(k[j]);
            f = k[j];
            break
        }
        e.nodeName.toUpperCase() == "UL" || e.nodeName.toUpperCase() == "OL" ? (this.list = d(e), this.clip = this.list.parents(".jcarousel-clip"), this.container = this.list.parents(".jcarousel-container")) : (this.container = d(e), this.list = this.container.find("ul,ol").eq(0), this.clip = this.container.find(".jcarousel-clip"));
        if (this.clip.size() === 0) this.clip = this.list.wrap("<div></div>").parent();
        if (this.container.size() === 0) this.container = this.clip.wrap("<div></div>").parent();
        f !== "" && this.container.parent()[0].className.indexOf("jcarousel-skin") == -1 && this.container.wrap('<div class=" ' + f + '"></div>');
        this.buttonPrev = d(".jcarousel-prev", this.container);
        if (this.buttonPrev.size() === 0 && this.options.buttonPrevHTML !== null) this.buttonPrev = d(this.options.buttonPrevHTML).appendTo(this.container);
        this.buttonPrev.addClass(this.className("jcarousel-prev"));
        this.buttonNext = d(".jcarousel-next", this.container);
        if (this.buttonNext.size() === 0 && this.options.buttonNextHTML !== null) this.buttonNext = d(this.options.buttonNextHTML).appendTo(this.container);
        this.buttonNext.addClass(this.className("jcarousel-next"));
        this.clip.addClass(this.className("jcarousel-clip")).css({
            position: "relative"
        });
        this.list.addClass(this.className("jcarousel-list")).css({
            overflow: "hidden",
            position: "relative",
            top: 0,
            margin: 0,
            padding: 0
        }).css(this.options.rtl ? "right" : "left", 0);
        this.container.addClass(this.className("jcarousel-container")).css({
            position: "relative"
        });
        !this.options.vertical && this.options.rtl && this.container.addClass("jcarousel-direction-rtl").attr("dir", "rtl");
        var l = this.options.visible !== null ? Math.ceil(this.clipping() / this.options.visible) : null,
            f = this.list.children("li"),
            g = this;
        if (f.size() > 0) {
            var i = 0,
                m = this.options.offset;
            f.each(function () {
                g.format(this, m++);
                i += g.dimension(this, l)
            });
            this.list.css(this.wh, i + 100 + "px");
            if (!b || b.size === void 0) this.options.size = f.size()
        }
        this.container.css("display", "block");
        this.buttonNext.css("display", "block");
        this.buttonPrev.css("display", "block");
        this.funcNext = function () {
            g.next()
        };
        this.funcPrev = function () {
            g.prev()
        };
        this.funcResize = function () {
            g.resizeTimer && clearTimeout(g.resizeTimer);
            g.resizeTimer = setTimeout(function () {
                g.reload()
            }, 100)
        };
        this.options.initCallback !== null && this.options.initCallback(this, "init");
        !c && d.browser.safari ? (this.buttons(!1, !1), d(window).bind("load.jcarousel", function () {
            g.setup()
        })) : this.setup()
    };
    var b = d.jcarousel;
    b.fn = b.prototype = {
        jcarousel: "0.2.8"
    };
    b.fn.extend = b.extend = d.extend;
    b.fn.extend({
        setup: function () {
            this.prevLast = this.prevFirst = this.last = this.first = null;
            this.animating = !1;
            this.tail = this.resizeTimer = this.timer = null;
            this.inTail = !1;
            if (!this.locked) {
                this.list.css(this.lt, this.pos(this.options.offset) + "px");
                var e = this.pos(this.options.start, !0);
                this.prevFirst = this.prevLast = null;
                this.animate(e, !1);
                d(window).unbind("resize.jcarousel", this.funcResize).bind("resize.jcarousel", this.funcResize);
                this.options.setupCallback !== null && this.options.setupCallback(this)
            }
        },
        reset: function () {
            this.list.empty();
            this.list.css(this.lt, "0px");
            this.list.css(this.wh, "10px");
            this.options.initCallback !== null && this.options.initCallback(this, "reset");
            this.setup()
        },
        reload: function () {
            this.tail !== null && this.inTail && this.list.css(this.lt, b.intval(this.list.css(this.lt)) + this.tail);
            this.tail = null;
            this.inTail = !1;
            this.options.reloadCallback !== null && this.options.reloadCallback(this);
            if (this.options.visible !== null) {
                var e = this,
                    a = Math.ceil(this.clipping() / this.options.visible),
                    c = 0,
                    d = 0;
                this.list.children("li").each(function (b) {
                    c += e.dimension(this,
                    a);
                    b + 1 < e.first && (d = c)
                });
                this.list.css(this.wh, c + "px");
                this.list.css(this.lt, - d + "px")
            }
            this.scroll(this.first, !1)
        },
        lock: function () {
            this.locked = !0;
            this.buttons()
        },
        unlock: function () {
            this.locked = !1;
            this.buttons()
        },
        size: function (e) {
            if (e !== void 0) this.options.size = e, this.locked || this.buttons();
            return this.options.size
        },
        has: function (e, a) {
            if (a === void 0 || !a) a = e;
            if (this.options.size !== null && a > this.options.size) a = this.options.size;
            for (var b = e; b <= a; b++) {
                var c = this.get(b);
                if (!c.length || c.hasClass("jcarousel-item-placeholder")) return !1
            }
            return !0
        },
        get: function (a) {
            return d(">.jcarousel-item-" + a, this.list)
        },
        add: function (a, c) {
            var f = this.get(a),
                k = 0,
                j = d(c);
            if (f.length === 0) for (var l, g = b.intval(a), f = this.create(a);;) {
                if (l = this.get(--g), g <= 0 || l.length) {
                    g <= 0 ? this.list.prepend(f) : l.after(f);
                    break
                }
            } else k = this.dimension(f);
            j.get(0).nodeName.toUpperCase() == "LI" ? (f.replaceWith(j), f = j) : f.empty().append(c);
            this.format(f.removeClass(this.className("jcarousel-item-placeholder")), a);
            j = this.options.visible !== null ? Math.ceil(this.clipping() / this.options.visible) : null;
            k = this.dimension(f, j) - k;
            a > 0 && a < this.first && this.list.css(this.lt, b.intval(this.list.css(this.lt)) - k + "px");
            this.list.css(this.wh, b.intval(this.list.css(this.wh)) + k + "px");
            return f
        },
        remove: function (a) {
            var c = this.get(a);
            if (c.length && !(a >= this.first && a <= this.last)) {
                var f = this.dimension(c);
                a < this.first && this.list.css(this.lt, b.intval(this.list.css(this.lt)) + f + "px");
                c.remove();
                this.list.css(this.wh, b.intval(this.list.css(this.wh)) - f + "px")
            }
        },
        next: function () {
            this.tail !== null && !this.inTail ? this.scrollTail(!1) : this.scroll((this.options.wrap == "both" || this.options.wrap == "last") && this.options.size !== null && this.last == this.options.size ? 1 : this.first + this.options.scroll)
       		             
          
        },
        prev: function () {
            this.tail !== null && this.inTail ? this.scrollTail(!0) : this.scroll((this.options.wrap == "both" || this.options.wrap == "first") && this.options.size !== null && this.first == 1 ? this.options.size : this.first - this.options.scroll)
        },
        scrollTail: function (a) {
            if (!this.locked && !this.animating && this.tail) {
                this.pauseAuto();
                var c = b.intval(this.list.css(this.lt)),
                    c = !a ? c - this.tail : c + this.tail;
                this.inTail = !a;
                this.prevFirst = this.first;
                this.prevLast = this.last;
                this.animate(c)
            }
        },
        scroll: function (a, c) {
              !this.locked && !this.animating && (this.pauseAuto(), this.animate(this.pos(a), c))
        },
        pos: function (a, c) {
            var f = b.intval(this.list.css(this.lt));
            if (this.locked || this.animating) return f;
            this.options.wrap != "circular" && (a = a < 1 ? 1 : this.options.size && a > this.options.size ? this.options.size : a);
            for (var d = this.first > a, j = this.options.wrap != "circular" && this.first <= 1 ? 1 : this.first, l = d ? this.get(j) : this.get(this.last), g = d ? j : j - 1, i = null, m = 0, o = !1, p = 0; d ? --g >= a : ++g < a;) {
                i = this.get(g);
                o = !i.length;
                if (i.length === 0 && (i = this.create(g).addClass(this.className("jcarousel-item-placeholder")), l[d ? "before" : "after"](i), this.first !== null && this.options.wrap == "circular" && this.options.size !== null && (g <= 0 || g > this.options.size))) l = this.get(this.index(g)), l.length && (i = this.add(g, l.clone(!0)));
                l = i;
                p = this.dimension(i);
                o && (m += p);
                if (this.first !== null && (this.options.wrap == "circular" || g >= 1 && (this.options.size === null || g <= this.options.size))) f = d ? f + p : f - p
            }
            for (var j = this.clipping(), n = [], v = 0, r = 0, l = this.get(a - 1), g = a; ++v;) {
                i = this.get(g);
                o = !i.length;
                if (i.length === 0) {
                    i = this.create(g).addClass(this.className("jcarousel-item-placeholder"));
                    if (l.length === 0) this.list.prepend(i);
                    else l[d ? "before" : "after"](i);
                    if (this.first !== null && this.options.wrap == "circular" && this.options.size !== null && (g <= 0 || g > this.options.size)) l = this.get(this.index(g)), l.length && (i = this.add(g, l.clone(!0)))
                }
                l = i;
                p = this.dimension(i);
                if (p === 0) throw Error("jCarousel: No width/height set for items. This will cause an infinite loop. Aborting...");
                this.options.wrap != "circular" && this.options.size !== null && g > this.options.size ? n.push(i) : o && (m += p);
                r += p;
                if (r >= j) break;
                g++
            }
            for (i = 0; i < n.length; i++) n[i].remove();
            m > 0 && (this.list.css(this.wh, this.dimension(this.list) + m + "px"), d && (f -= m, this.list.css(this.lt, b.intval(this.list.css(this.lt)) - m + "px")));
            m = a + v - 1;
            if (this.options.wrap != "circular" && this.options.size && m > this.options.size) m = this.options.size;
            if (g > m) {
                v = 0;
                g = m;
                for (r = 0; ++v;) {
                    i = this.get(g--);
                    if (!i.length) break;
                    r += this.dimension(i);
                    if (r >= j) break
                }
            }
            g = m - v + 1;
            this.options.wrap != "circular" && g < 1 && (g = 1);
            if (this.inTail && d) f += this.tail, this.inTail = !1;
            this.tail = null;
            if (this.options.wrap != "circular" && m == this.options.size && m - v + 1 >= 1 && (d = b.intval(this.get(m).css(!this.options.vertical ? "marginRight" : "marginBottom")), r - d > j)) this.tail = r - j - d;
            if (c && a === this.options.size && this.tail) f -= this.tail, this.inTail = !0;
            for (; a-- > g;) f += this.dimension(this.get(a));
            this.prevFirst = this.first;
            this.prevLast = this.last;
            this.first = g;
            this.last = m;
            return f
        },
        animate: function (a, c) {
            if (!this.locked && !this.animating) {
                this.animating = !0;
                var b = this,
                    k = function () {
                        b.animating = !1;
                        a === 0 && b.list.css(b.lt, 0);
                        !b.autoStopped && (b.options.wrap == "circular" || b.options.wrap == "both" || b.options.wrap == "last" || b.options.size === null || b.last < b.options.size || b.last == b.options.size && b.tail !== null && !b.inTail) && b.startAuto();
                        b.buttons();
                        b.notify("onAfterAnimation");
                        if (b.options.wrap == "circular" && b.options.size !== null) for (var c = b.prevFirst; c <= b.prevLast; c++) c !== null && !(c >= b.first && c <= b.last) && (c < 1 || c > b.options.size) && b.remove(c)
                    };
                this.notify("onBeforeAnimation");
                if (!this.options.animation || c === !1) this.list.css(this.lt, a + "px"), k();
                else {
                    var j = !this.options.vertical ? this.options.rtl ? {
                        right: a
                    } : {
                        left: a
                    } : {
                        top: a
                    }, k = {
                        duration: this.options.animation,
                        easing: this.options.easing,
                        complete: k
                    };
                    if (d.isFunction(this.options.animationStepCallback)) k.step = this.options.animationStepCallback;
                    this.list.animate(j, k)
                }
            }
        },
        startAuto: function (a) {
            if (a !== void 0) this.options.auto = a;
            if (this.options.auto === 0) return this.stopAuto();
            if (this.timer === null) {
                this.autoStopped = !1;
                var b = this;
                this.timer = window.setTimeout(function () {
                    b.next()
                }, this.options.auto * 1E3)
            }
        },
        stopAuto: function () {
            this.pauseAuto();
            this.autoStopped = !0
        },
        pauseAuto: function () {
            if (this.timer !== null) window.clearTimeout(this.timer), this.timer = null
        },
        buttons: function (a, b) {
            if (a == null && (a = !this.locked && this.options.size !== 0 && (this.options.wrap && this.options.wrap != "first" || this.options.size === null || this.last < this.options.size), !this.locked && (!this.options.wrap || this.options.wrap == "first") && this.options.size !== null && this.last >= this.options.size)) a = this.tail !== null && !this.inTail;
            if (b == null && (b = !this.locked && this.options.size !== 0 && (this.options.wrap && this.options.wrap != "last" || this.first > 1), !this.locked && (!this.options.wrap || this.options.wrap == "last") && this.options.size !== null && this.first == 1)) b = this.tail !== null && this.inTail;
            var c = this;
            this.buttonNext.size() > 0 ? (this.buttonNext.unbind(this.options.buttonNextEvent + ".jcarousel", this.funcNext), a && this.buttonNext.bind(this.options.buttonNextEvent + ".jcarousel", this.funcNext),
            this.buttonNext[a ? "removeClass" : "addClass"](this.className("jcarousel-next-disabled")).attr("disabled", a ? !1 : !0), this.options.buttonNextCallback !== null && this.buttonNext.data("jcarouselstate") != a && this.buttonNext.each(function () {
                c.options.buttonNextCallback(c, this, a)
            }).data("jcarouselstate", a)) : this.options.buttonNextCallback !== null && this.buttonNextState != a && this.options.buttonNextCallback(c, null, a);
            this.buttonPrev.size() > 0 ? (this.buttonPrev.unbind(this.options.buttonPrevEvent + ".jcarousel", this.funcPrev),
            b && this.buttonPrev.bind(this.options.buttonPrevEvent + ".jcarousel", this.funcPrev), this.buttonPrev[b ? "removeClass" : "addClass"](this.className("jcarousel-prev-disabled")).attr("disabled", b ? !1 : !0), this.options.buttonPrevCallback !== null && this.buttonPrev.data("jcarouselstate") != b && this.buttonPrev.each(function () {
                c.options.buttonPrevCallback(c, this, b)
            }).data("jcarouselstate", b)) : this.options.buttonPrevCallback !== null && this.buttonPrevState != b && this.options.buttonPrevCallback(c, null, b);
            this.buttonNextState = a;
            this.buttonPrevState = b
        },
        notify: function (a) {
            var b = this.prevFirst === null ? "init" : this.prevFirst < this.first ? "next" : "prev";
            this.callback("itemLoadCallback", a, b);
            this.prevFirst !== this.first && (this.callback("itemFirstInCallback", a, b, this.first), this.callback("itemFirstOutCallback", a, b, this.prevFirst));
            this.prevLast !== this.last && (this.callback("itemLastInCallback", a, b, this.last), this.callback("itemLastOutCallback", a, b, this.prevLast));
            this.callback("itemVisibleInCallback", a, b, this.first, this.last, this.prevFirst,
            this.prevLast);
            this.callback("itemVisibleOutCallback", a, b, this.prevFirst, this.prevLast, this.first, this.last)
        },
        callback: function (a, b, c, k, j, l, g) {
            if (!(this.options[a] == null || typeof this.options[a] != "object" && b != "onAfterAnimation")) {
                var i = typeof this.options[a] == "object" ? this.options[a][b] : this.options[a];
                if (d.isFunction(i)) {
                    var m = this;
                    if (k === void 0) i(m, c, b);
                    else if (j === void 0) this.get(k).each(function () {
                        i(m, this, k, c, b)
                    });
                    else for (var a = function (a) {
                        m.get(a).each(function () {
                            i(m, this, a, c, b)
                        })
                    }, o = k; o <= j; o++) o !== null && !(o >= l && o <= g) && a(o)
                }
            }
        },
        create: function (a) {
            return this.format("<li></li>", a)
        },
        format: function (a, b) {
            for (var a = d(a), c = a.get(0).className.split(" "), k = 0; k < c.length; k++) c[k].indexOf("jcarousel-") != -1 && a.removeClass(c[k]);
            a.addClass(this.className("jcarousel-item")).addClass(this.className("jcarousel-item-" + b)).css({
                "float": this.options.rtl ? "right" : "left",
                "list-style": "none"
            }).attr("jcarouselindex", b);
            return a
        },
        className: function (a) {
            return a + " " + a + (!this.options.vertical ? "-horizontal" : "-vertical")
        },
        dimension: function (a, c) {
            var f = d(a);
            if (c == null) return !this.options.vertical ? f.outerWidth(!0) || b.intval(this.options.itemFallbackDimension) : f.outerHeight(!0) || b.intval(this.options.itemFallbackDimension);
            else {
                var k = !this.options.vertical ? c - b.intval(f.css("marginLeft")) - b.intval(f.css("marginRight")) : c - b.intval(f.css("marginTop")) - b.intval(f.css("marginBottom"));
                d(f).css(this.wh, k + "px");
                return this.dimension(f)
            }
        },
        clipping: function () {
            return !this.options.vertical ? this.clip[0].offsetWidth - b.intval(this.clip.css("borderLeftWidth")) - b.intval(this.clip.css("borderRightWidth")) : this.clip[0].offsetHeight - b.intval(this.clip.css("borderTopWidth")) - b.intval(this.clip.css("borderBottomWidth"))
        },
        index: function (a, b) {
            if (b == null) b = this.options.size;
            return Math.round(((a - 1) / b - Math.floor((a - 1) / b)) * b) + 1
        }
    });
    b.extend({
        defaults: function (b) {
            return d.extend(a, b || {})
        },
        intval: function (a) {
            a = parseInt(a, 10);
            return isNaN(a) ? 0 : a
        },
        windowLoaded: function () {
            c = !0
        }
    });
    d.fn.jcarousel = function (a) {
        if (typeof a == "string") {
            var c = d(this).data("jcarousel"),
                f = Array.prototype.slice.call(arguments,
                1);
            return c[a].apply(c, f)
        } else return this.each(function () {
            var c = d(this).data("jcarousel");
            c ? (a && d.extend(c.options, a), c.reload()) : d(this).data("jcarousel", new b(this, a))
        })
    }
})(jQuery);
(function (d) {
    d.fn.swipe = function (a) {
        if (!this) return !1;
        var c = {
            fingers: 1,
            threshold: 75,
            swipe: null,
            swipeLeft: null,
            swipeRight: null,
            swipeUp: null,
            swipeDown: null,
            swipeStatus: null,
            click: null,
            triggerOnTouchEnd: !0,
            allowPageScroll: "auto"
        }, b = "left",
            e = "right",
            h = "up",
            f = "down",
            k = "none",
            j = "horizontal",
            l = "vertical",
            g = "auto",
            i = "start",
            m = "move",
            o = "end",
            p = "cancel",
            n = "start";
        if (a.allowPageScroll == void 0 && (a.swipe != void 0 || a.swipeStatus != void 0)) a.allowPageScroll = k;
        a && d.extend(c, a);
        return this.each(function () {
            function a(b) {
                n = i;
                t = b.touches.length;
                distance = 0;
                direction = null;
                t == c.fingers ? (s.x = q.x = b.touches[0].pageX, s.y = q.y = b.touches[0].pageY, c.swipeStatus && w(b, n)) : x(b)
            }
            function r(a) {
                if (!(n == o || n == p)) {
                    q.x = a.touches[0].pageX;
                    q.y = a.touches[0].pageY;
                    direction = y();
                    t = a.touches.length;
                    n = m;
                    var d = direction;
                    if (c.allowPageScroll == k) a.preventDefault();
                    else {
                        var i = c.allowPageScroll == g;
                        switch (d) {
                        case b:
                            (c.swipeLeft && i || !i && c.allowPageScroll != j) && a.preventDefault();
                            break;
                        case e:
                            (c.swipeRight && i || !i && c.allowPageScroll != j) && a.preventDefault();
                            break;
                        case h:
                            (c.swipeUp && i || !i && c.allowPageScroll != l) && a.preventDefault();
                            break;
                        case f:
                            (c.swipeDown && i || !i && c.allowPageScroll != l) && a.preventDefault()
                        }
                    }
                    t == c.fingers ? (distance = z(), c.swipeStatus && w(a, n, direction, distance), !c.triggerOnTouchEnd && distance >= c.threshold && (n = o, w(a, n), x(a))) : (n = p, w(a, n), x(a))
                }
            }
            function A(a) {
                a.preventDefault();
                distance = z();
                direction = y();
                c.triggerOnTouchEnd ? (n = o, t == c.fingers && q.x != 0 ? distance >= c.threshold || (n = p) : n = p, w(a, n), x(a)) : n == m && (n = p, w(a, n), x(a))
            }
            function x() {
                t = 0;
                s.x = 0;
                s.y = 0;
                q.x = 0;
                q.y = 0
            }
            function w(a, d) {
                c.swipeStatus && c.swipeStatus.call(u, a, d, direction || null, distance || 0);
                d == p && c.click && t == 1 && (isNaN(distance) || distance == 0) && c.click.call(u, a, a.target);
                if (d == o) switch (c.swipe && c.swipe.call(u, a, direction, distance), direction) {
                case b:
                    c.swipeLeft && c.swipeLeft.call(u, a, direction, distance);
                    break;
                case e:
                    c.swipeRight && c.swipeRight.call(u, a, direction, distance);
                    break;
                case h:
                    c.swipeUp && c.swipeUp.call(u, a, direction, distance);
                    break;
                case f:
                    c.swipeDown && c.swipeDown.call(u, a, direction,
                    distance)
                }
            }
            function z() {
                return Math.round(Math.sqrt(Math.pow(q.x - s.x, 2) + Math.pow(q.y - s.y, 2)))
            }
            function y() {
                var a;
                a = Math.round(Math.atan2(q.y - s.y, s.x - q.x) * 180 / Math.PI);
                a < 0 && (a = 360 - Math.abs(a));
                return a <= 45 && a >= 0 ? b : a <= 360 && a >= 315 ? b : a >= 135 && a <= 225 ? e : a > 45 && a < 135 ? f : h
            }
            var u = d(this),
                t = 0,
                s = {
                    x: 0,
                    y: 0
                }, q = {
                    x: 0,
                    y: 0
                };
            try {
                this.addEventListener("touchstart", a, !1), this.addEventListener("touchmove", r, !1), this.addEventListener("touchend", A, !1), this.addEventListener("touchcancel", x, !1)
            } catch (B) {}
        })
    }
})(jQuery);
jQuery.fn.extend({
    captify: function (d) {
        var a = $.extend({
            speedOver: "fast",
            speedOut: "normal",
            hideDelay: 500,
            animation: "slide",
            prefix: "",
           //opacity: "0.45",
            className: "caption-bottom",
            position: "bottom",
            spanWidth: "100%"
        }, d);
        $(this).each(function () {
            var c = this;
            $(this).load(function () {
                if (c.hasInit) return !1;
                c.hasInit = !0;
                var b = !1,
                    d = !1,
                    h = $("#" + $(this).attr("rel")),
                    f = !h.length ? $(this).attr("alt") : h.html();
                h.remove();
                var k = (this.parent && this.parent.tagName == "a" ? this.parent : $(this)).wrap("<div></div>").parent().css({
                    overflow: "hidden",
                    padding: 0,
                    fontSize: 0.1
                }).addClass("caption-wrapper").width($(this).width()).height($(this).height());
                $.map(["top", "right", "bottom", "left"], function (a) {
                    k.css("margin-" + a, $(c).css("margin-" + a));
                    $.map(["style", "width", "color"], function (b) {
                        b = "border-" + a + "-" + b;
                        k.css(b, $(c).css(b))
                    })
                });
                $(c).css({
                    border: "0 none"
                });
                var j = $("div:last", k.append("<div></div>")).addClass(a.className),
                    l = $("div:last", k.append("<div></div>")).addClass(a.className).append(a.prefix).append(f);
                $("*", k).css({
                    margin: 0
                }).show();
                h = jQuery.browser.msie ? "static" : "relative";
                j.css({
                    zIndex: 1,
                    position: h,
                    opacity: a.animation == "fade" ? 0 : a.opacity,
                    width: a.spanWidth
                });
                a.position == "bottom" && (f = parseInt(j.css("border-top-width").replace("px", "")) + parseInt(l.css("padding-top").replace("px", "")) - 1, l.css("paddingTop", f));
                
                
                
                l.css({
                    position: h,
                    zIndex: 2,
                    background: "none",
                    border: "0 none",
                    opacity: a.animation == "fade" ? 0 : 1,
                    width: a.spanWidth,

                });
                j.width(l.outerWidth());
                j.height(l.height());
                var h = a.position == "bottom" && jQuery.browser.msie ? -4 : 0,
                    g = a.position == "top" ? {
                        hide: -$(c).height() - j.outerHeight() - 1,
                        show: -$(c).height()
                    } : {
                        hide: 0,
                        show: -j.outerHeight() + h
                    };
                l.css("marginTop", - j.outerHeight());
                j.css("marginTop", "0px");
   
                
                var i = function () {
                    !b && !d && (j.animate(a.animation == "fade" ? {
                        opacity: 0
                    } : {
                        marginTop: g.hide
                    }, a.speedOut), a.animation == "fade" && l.animate({
                        opacity: 0
                    }, a.speedOver))
                };
                a.animation != "always-on" && ($(this).hover(function () {
                    d = !0;
                    b || (j.animate(a.animation == "fade" ? {
                        opacity: a.opacity
                    } : {
                        marginTop: g.show
                    }, a.speedOver), a.animation == "fade" && l.animate({
                        opacity: 1
                    }, a.speedOver / 2))
                }, function () {
                    d = !1;
                    window.setTimeout(i, a.hideDelay)
                }), $("div", k).hover(function () {
                    b = !0
                }, function () {
                    b = !1;
                    window.setTimeout(i, a.hideDelay)
                }))
            });
            (this.complete || this.naturalWidth > 0) && $(c).trigger("load")
        })
    }
});